package it.timesheet.domain.repository;

import it.timesheet.domain.entity.Proggeto;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProggetoRepository extends JpaRepository<Proggeto, Long> {
}
